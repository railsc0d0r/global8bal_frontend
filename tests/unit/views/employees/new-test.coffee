`import { moduleFor, test } from 'ember-qunit'`

moduleFor 'view:employees/new', 'Unit | View | employees/new'

# Replace this with your real tests.
test 'it exists', (assert) ->
  view = @subject()
  assert.ok view
